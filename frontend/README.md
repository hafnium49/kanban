# Kanban Board

A project management board with drag-and-drop cards across 5 workflow columns.

## Setup

```bash
cd frontend
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Tests

```bash
npm run test        # unit tests (Vitest)
npm run test:e2e    # integration tests (Playwright)
```
